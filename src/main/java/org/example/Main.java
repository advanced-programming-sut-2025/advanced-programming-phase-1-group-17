package org.example;

import org.example.models.FlowerType;
import org.example.models.enums.ToolType;
import org.example.views.AppView;

import java.awt.*;

public class Main {
    public static void main(String[] args) {
        (new AppView()).run();
    }
}
